const router = require('express').Router();

//1. Going to be a router.post which will create a new post

//2. router.put which will be used for editing a post

//3. router.delete which is used for deleting a post

module.exports = router;